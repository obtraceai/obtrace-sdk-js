# MCP

This repository publishes `mcp.json` for tooling that indexes SDK context and docs resources.

## File
- `mcp.json`

## Goal
- Make repository structure and key docs machine-readable for assistant/tool pipelines.
